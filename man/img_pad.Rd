% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/masks.R
\name{img_pad}
\alias{img_pad}
\title{Add padding (border) to image}
\usage{
img_pad(img, n = 10, value = NULL)
}
\arguments{
\item{img}{Matrix or array. Image from img_load() or binary mask}

\item{n}{Integer. Number of pixels to add on each side. Default 10}

\item{value}{Numeric or logical. Value for padding pixels.
\itemize{
\item For grayscale (0-255): Default 255 (white)
\item For masks: Default FALSE (background)
\item For color: Specify as c(R, G, B) or single value (default 255)
}}
}
\value{
Padded image with dimensions increased by 2*n in each direction
}
\description{
Adds a border of pixels around an image. Useful for ensuring objects
don't touch image edges before flood fill.
}
\examples{
\dontrun{
img <- img_load("leaf.png")
img_padded <- img_pad(img, n = 20)
dim(img)        # e.g., 100 x 100
dim(img_padded) # 140 x 140

# Pad a mask
mask <- img_mask(img)
mask_padded <- img_pad(mask, n = 10, value = FALSE)
}
}
