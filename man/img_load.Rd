% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/masks.R
\name{img_load}
\alias{img_load}
\title{Load image as grayscale matrix}
\usage{
img_load(path, color = FALSE, flip = FALSE)
}
\arguments{
\item{path}{Character. Path to image file}

\item{color}{Logical. Keep as RGB color? Default FALSE (convert to grayscale)}

\item{flip}{Logical. Flip vertically? (Images are top-down, coords bottom-up). Default TRUE}
}
\value{
If color=FALSE: Integer matrix (nrow = height, ncol = width) with values 0-255
If color=TRUE: Integer array (nrow = height, ncol = width, 3) with RGB values 0-255
}
\description{
Reads any image format and returns a grayscale matrix of integers (0-255).
Useful for custom image processing before outline extraction.
}
\examples{
\dontrun{
# Load as grayscale
img <- img_load("leaf.jpg")
dim(img)  # height x width

# Load as color
img_rgb <- img_load("leaf.jpg", color = TRUE)
dim(img_rgb)  # height x width x 3

# For most uses, just call from_mask() directly
outline <- from_mask("leaf.jpg")
}
}
