% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/masks.R
\name{img_mask}
\alias{img_mask}
\title{Convert image to binary mask using flood fill}
\usage{
img_mask(img, fuzz = 20, invert = TRUE)
}
\arguments{
\item{img}{Integer matrix (0-255) from img_load()}

\item{fuzz}{Integer. Fuzz/tolerance for flood fill (0-255). Default 20
Controls how similar pixels must be to be considered background.
\itemize{
\item Low (5-10): Very strict, only very similar pixels
\item Medium (20-30): Reasonable for most cases
\item High (50+): Aggressive, may include object edges
}}

\item{invert}{Logical. Invert the mask? Default TRUE
\itemize{
\item TRUE: object = FALSE (white), background = TRUE (black)
\item FALSE: object = TRUE (black), background = FALSE (white)
}}
}
\value{
Logical matrix (TRUE/FALSE depending on invert setting)
}
\description{
Creates a binary mask by flood-filling the background from top-left corner,
then setting all non-background pixels to black (TRUE). This handles complex
backgrounds and multiple objects uniformly.
}
\examples{
\dontrun{
# Standard (inverted): white object on black background
img <- img_load("leaf.png")
mask <- img_mask(img)

# Non-inverted: black object on white background
mask_std <- img_mask(img, invert = FALSE)

# Adjust fuzz for noisy backgrounds
mask <- img_mask(img, fuzz = 30)
}
}
